# Fabric Data Agent Chat UI

React + Tailwind UI for the Stock Pulse / Fabric Data Agent backend.

## Local run

```bash
npm install
npm run dev
```

Start the backend from the `backend` folder (see root [README](../README.md)).

## Production deploy

See the root [README](../README.md) for GitHub → Vercel (frontend) + Railway/Render (backend) setup.
